# 20 · 设计决策与跨文档对齐记录 (Decisions & Reconciliation)

> **权威性**：本文件裁定所有跨文档结构冲突与开放问题，是设计包"自洽可落地"的最终依据。任何下游文档与之冲突，以本文件为准。
> 依据「全程不需要人为干预」原则，所有需"主创拍板"项**已由本项目代为决策并记录理由**；如需推翻，改本文件对应条目即可。
> 上游：`00-DESIGN-BRIEF.md`。影响：05/06/08/09/10/11/14/15 + `19-risk-register.md` + `00-README.md` 决策日志。

---

## 0. 对齐总原则

1. **数值单一真源 = `14-game-balance-and-math.md`**（公式与【平衡参数注册表 P001–P037】）。机制文档（05–09）的 `【可调参数】` 默认值若与 14 冲突，**以 14 为准**；14 未定义的，以机制文档为准并在 14 补登。
2. **结构/阶段 = 机制文档为准**：阶段阶梯、命中等机制结构以 05–09 为权威，14 的对应表格须对齐机制文档（14 已自标"[待与 09 对齐]"）。
3. **术语 = `00-DESIGN-BRIEF.md §6`**。
4. 所有随机走可种子化 PRNG（C3）；所有内容数据驱动（C6）。

---

## 1. 已裁定的结构冲突（Reconciliation）

### R1. 修炼阶段数：7 阶 ✅（采纳 09）
- **冲突**：09 用 7 阶（凡骨→淬皮→锻骨→通脉→凝丹→破丹→化神→飞升前夜）；14 §8 用 5 阶（0–5）。
- **裁定**：**采纳 7 阶**（09 的模型）。理由：09 是进阶专责文档，围绕 7 阶设计了经脉子进度与 ~18h 节奏；粒度更适合"险而可破"曲线。14 §8.1 阶段表须扩为 7 阶，`StageQiCap` 按 09（100/200/400/700/1100/1600/2200，×1.8）。
- **待编辑**：`14` §8.1 阶段表改 7 行；`14` §8 各处 stage 上限对齐；`05` §5.2 雷类型解锁按 7 阶重排（紫雷劫池在 stage 7）。

### R2. 天劫倒计时单位：混合（日为准备窗 + 秒为临战） ✅
- **冲突**：05 用实时秒（90–360s）；14 用游戏日（`T_trib=7−min(stage,4)`=3–7 日）。
- **裁定**：**混合**。`T_trib(stage)`（14 的游戏日）= **备劫准备窗**（玩家有完整种田周期补阵/炼丹/控毒）；准备窗最后一日归零时，进入 05 的**实时秒级临战**（`FreezeDuration` 1.5s 定格 + 实时波次承劫）。即"日级备战 + 秒级渡劫"。
- **待编辑**：`05` §1.2 把 `BaseCountdown/StageStep/MaxCountdown`（秒）定位为"临战最后倒计时"；新增"准备窗=游戏日"说明并引用 14 `T_trib`。`14` `T_trib` 保持日单位，补注"最后一日转实时秒"。

### R3. `temperingEff(stage)` 递减：采纳 14 ✅
- **冲突**：14 §6.2 引入 `temperingEff=1.1−0.1×stage`（高阶每点伤害换更少修为，防后期血量 farm）；05 未建模。
- **裁定**：**采纳 14**，并入 05 `TemperingGain` 公式。
- **待编辑**：`05` §4.2 `TemperingGain = BaseTempering × ExposureCoeff × StageMultiplier × QualityBonus × temperingEff(stage)`，并注明 `temperingEff` 见 14 §6.2（P022/023）。

### R4. 天雷 targeting 权重：统一为乘性 ✅
- **冲突**：05 用加性 `W = Conductivity + MetalAttraction + ArrayModifier + PlayerProximity + EpicenterBias` ×jitter；14 §5.1 用乘性 `W = (1+metalAttract)(1+conductivity)×arrayModifier×playerProximity×(1+noise)`。
- **裁定**：**统一为乘性**（14）。理由：乘性防负、各项独立缩放清晰、与"绝缘阵趋零"语义一致。05 的各项物理建模（金属性草半径吸引 falloff、阵法乘积叠加、玩家邻近衰减）**保留为各项的内部计算**，仅把"各项如何合成总权重"改为乘性。
- **量纲**：各乘子规范化为非负倍率。`conductivity` 用 05/08 的 0.1–1.8 区间（作为 `1+` 项则映射为 1.1–2.8 倍率）；14 的加性小值（−0.2–0.5）统一改写为 0.8–1.5 倍率族（见 R5）。
- **待编辑**：`05` §3.2 权重合成改为乘性（各项内部算法不变）；`14` §5.2 conductivity 默认值对齐 08 的 0.1–1.8 语义。

### R5. 导电性量纲：乘性倍率族 ✅
- **冲突**：05/08 用乘性 0.1–1.8；14 用加性 −0.2–0.5。
- **裁定**：**采纳 05/08 的 0.1–1.8 作为 `Conductivity` 字段值**（语义=该格基础导电倍率，水 1.8 / 泥 1.0 / 干沙 0.5 / 绝缘垫 0.1 / 金属矿 1.5）。在乘性权重公式中以 `(conductivity)` 直接作为一项乘子（绝缘垫 0.1 即把该格权重压到 1/10）。
- **待编辑**：`14` §5.2 conductivity 行改为 08 的 0.1–1.8 表；`11` `Tile.conductivity` 毫点注释补"映射 0.0–2.0 倍率"。

### R6. 药性模型：内部四轴 + 玩家面一维投影 ✅
- **冲突**：06 用一维"寒热轴"（PropertyAxis −2..+2）；14 §9 与 11 schema 用四轴 `[cold,hot,warm,neutral]`。
- **裁定**：**四轴为内部真源**（11 `PropertyVector` + 14 §9），**一维为玩家面投影**。投影规则：`寒热轴 = (hot − cold)`（warm 微调、neutral 不参与主轴）。丹炉平衡条显示一维主轴（玩家易读），炸炉主判定用 `|hot−cold|`（14 §9.3），丹方匹配用完整四轴（涌现来源）。
- **待编辑**：`06` §2 补"四轴内部 / 一维投影"说明；`06` 炸炉判定 `|PropertySum|` 改述为 `|hot−cold|` 投影并引 14 §9.3。

### R7. 炸炉阈值：采纳 14 ✅
- **冲突**：06 = 25（平）+ ConflictThreshold 15；14 §9.3 = `14 + 2×stage`。
- **裁定**：**采纳 14**（阶段缩放）。`ExplosionThreshold = 14 + 2×stage`（14 P034）。ConflictThreshold（警告亚阈）= `ExplosionThreshold × 0.6`。
- **待编辑**：`06` §5.2 参数改为引 14 P034。

### R8. 丹毒衰减单位：采纳 14（/日）✅
- **冲突**：06 `PassiveDecayRate=0.05/游戏分钟`；14 `decayBase=2.0/日`。
- **裁定**：**采纳 14** `2.0 P/日`（06 的 0.05/分钟折算 72/日，远超 100 上限，不合理）。
- **待编辑**：`06` §1.2 改 `PassiveDecayRate = 2.0/游戏日`（引 14 P005）。

### R9. 雷伤基值：采纳 14 ✅
- **冲突**：05 `BaseLightningDamage=35`（平）；14 `baseDamage=12+8×stage`。
- **裁定**：**采纳 14**（阶段缩放）。stage1=20（HP100 的 1/5，3 雷可控）更平衡；05 的 35 在 stage1 过致命。
- **待编辑**：`05` §4.3 `BaseLightningDamage` 改述为 `bolt.baseDamage(stage)=12+8×stage`（引 14 P017/018），`StageMultiplier` 含义对齐。

### R10. 淬体收益基数：采纳 14（伤害比例制）+ 保留 05 擦弹 ✅
- **冲突**：05 `TemperingGain=BaseTempering(10)×exposure×stage×quality`（固定基数）；14 `temperingGain=damageActuallyTaken×temperingEff×nearDeathBonus`（伤害比例制）。
- **裁定**：**采纳 14 的伤害比例制为基数**（奖励"实受伤害"，更贴"以雷淬体"），**叠加 05 的擦弹 `QualityBonus`** 与 `ExposureCoeff`（命中类型）。
- 统一公式：`TemperingGain = damageActuallyTaken × ExposureCoeff × temperingEff(stage) × nearDeathBonus(finalHP) × QualityBonus(PerfectBlock)`。
- **待编辑**：`05` §4.2 重写公式为上述统一式；`14` §6.2 注明并入 `ExposureCoeff/QualityBonus`（引 05）。

---

## 2. 已裁定的设计开放问题（Design Decisions）

> 源自各文档"开放问题"节。逐条决策 + 理由。编号 D-NN 与 `00-README.md §6` 决策日志同步。
> **`19-risk-register.md §5` 的 Q1–Q8 已全部在此裁定**：Q1=D-03 死亡、Q2=D-04 交易、Q3=难度锚点、Q4=D-02 技术栈、Q5=付费、Q6=D-美术、Q7=平台、Q8=D-14 结局。`19 §4.1`"待人类拍板"项以此为准（本项目按"无人干预"原则已代决）。

| ID | 问题 | 决策 | 理由 |
|----|------|------|------|
| D-03 | 死亡模型 | **混合制 C**（死亡=损失 50% 流动资产[修为/丹药/材料]，**保留知识资产**[残卷/阶段/已解锁丹方/经脉]，不回档、不允许 save-scum）；roguelite meta 默认关 | 采纳 `19 Q1`/`16 §7`（优于 09 §7.2 的读档制）：读档会纵容 save-scum 稀释"凡人脆弱"(Pillar 1)；混合制让死亡成为通胀泄压阀 + 学习曲线不归零，服务 C5"可挽回的局部失败"；proxy"再来一次≥70%"可达 |
| D-05 | 炼丹投料 | **实时反应式** | 火候解谜紧张感，贴核心张力；非 Potion Craft 离散步（06 §3.3） |
| D-06 | 擦弹 PerfectBlock | **保留** | 高玩核心技巧，"险而可破"曲线的体现（05 §4.4） |
| D-07 | 模式切换定格 | **1.5s 硬定格，不允许重布阵** | 保天劫不可控张力（05 §2.2） |
| D-08 | 凡间作物 | **保留** | 序章教学 + 饥饿度压力 + 凡/仙对比叙事（08 Q1） |
| D-09 | 药性维度 | **四轴内部 + 一维投影**（见 R6） | 平衡玩家可读性与涌现深度 |
| D-10 | 一击必杀保护 OSI | **stage ≤ 2 保留** | 防开局劝退，高阶取消增硬核（05 Q3） |
| D-11 | 主角自定义 | **姓名 + 性别 + 肖像**（无背景选项） | 控制范围；叙事主角是"无名穿越者" |
| D-12 | 角色命名 | **采纳** 萧无极(胜者/主题对手) / 忘言叟(隐修NPC/引导) / 了尘(货郎/经济接口)；败者修士代号"逆" | 已在 02 落地，命名稳定 |
| D-13 | 阵营命名 | **采纳** 太一宗(正)/丹鼎宗(炼丹正)/血煞门(魔) | 02/03 已用 |
| D-14 | 结局数量 | **4 主结局**（正·飞升 / 真·凡骨归田 / 暗·以身饲天 / 暗·屠龙成新天道）+ 低阶飞升(次优) + 归隐(隐藏) | 每结局主题完成、无坏结局（02） |
| D-15 | Hades 式死亡解锁 Lore | **MVP 后**（post-MVP） | 极丰富叙事但增开发量；先做基础失败碎片 |
| D-16 | 心雷劫 | **MVP=风险数值条；post-MVP=独立识海小关** | 控范围（02/09） |
| D-17 | 天道终局台词 | **极简/濒死视听为主，仅一句画外音** | 保天道"无人脸、以天象显形"的压迫感（03） |
| D-18 | 战力↔境界映射 | **不显式映射** | 保凡人视角，境界仅作背景压迫力（03） |
| D-19 | 经脉加成 | **部分可选**（玩家选开辟顺序） | build 差异化（09 Q3） |
| D-20 | 走火入魔结局 | **保留为结局**（走火丹赌徒归宿） | 主题代价；可恢复 buff 作为更软备选留档 |
| D-21 | 种子稀缺度 | **偏严**（凡人挣扎感） | C5；留种为主稳定来源（08 Q3） |
| D-22 | MVP 天象事件数 | **5 个核心**（灵气潮汐/妖兽潮/魔修过境/灵气枯竭/游方散仙）+ 因果链最小版 | 范围控制（09 Q4） |
| D-23 | 季节长度 | **28 天/季**（对齐星露谷）；监测 tier5 灵草(180 日熟)是否需灵脉加速 | 节奏基准（08 Q5） |
| D-24 | 付费 vs 开源（= `19` Q5） | **优先开源**：代码 MIT/Apache-2.0、字体 OFL（霞鹜文楷/思源）、内容 CC-BY 4.0；市场广泛好评后再考虑付费正式版/DLC | 用户指令 + 国内独立游戏开源更易建社区口碑；开源不阻塞商业化（可后续双轨） |
| D-25 | 游戏语言（= `19` 新增） | **全程中文（zh-CN）**，面向国内市场；含 i18n 键值层 + 内置 CJK 字体 + IME 合成 | 用户指令；工程方案见 `10` §13 / `00` C8 |
| D-26 | 主角路线定位 | **主角是无灵根凡人转走没落体修**；《偷天换劫诀》是极端雷劫炼体残卷，不是灵修功法，也不是单纯魔道邪术 | 用户修正：灵修靠灵根天资，体修靠苦练/意志/资源；主角开局绝灵但目标是体修成长 |
| D-27 | 天劫触发哲学 | **主动引劫为核心**；阶段满、天道因果账、大限压力只触发催讨式倒计时 | 对齐"我命由我不由天"；避免旧版"修为满被动强制"削弱主角选择 |
| D-28 | 体修短板补足 | **体修近战 + 阵法控场 + 守田兽**；阵法来自主角现代数理直觉，守田兽是农庄实用创新而非传统御兽 | 体修缺远程/法术，主角用穿越者知识和农庄经营补短板 |
| D-29 | 寿元与大限 | **加入大限倒计时**；体修突破延寿，但主动引劫增加天道因果，终局是与天道对弈 | 强化凡骨时间压力，防止无限发育，并服务终局主题 |
| D-30 | 飞升后是否强制结束 | **不强制结束**；终极飞升达成后，玩家可二选一：`飞升离界` 进入结局归档，或 `留驻此界` 转入"凡骨归田/镇守人间"后终局生活模式 | 对齐用户希望保留平淡日常与人间责任感；避免玩家因喜爱农庄生活而被终局强退，同时保留飞升作为完整收束 |
| D-31 | 平台定位 | **离线单机多端同核**：浏览器是开发、测试与公开展示首发载体；正式游玩目标继续扩到桌面端（Windows/Linux 优先），移动端后置评估 | 纯代码项目需要先用浏览器形态降低验证与传播成本，再把已验证的单机能力封装为桌面版本；平台扩展不得反向绑架核心 sim 设计 |
| D-32 | 版本发展策略 | **先作品集 MVP，后独立游戏首版，再以 Patch/DLC 持续补深** | 对齐用户“先跑起来、先惊艳、再持续靠近 Stardew-like 长期目标”的要求，避免首版被横向扩写拖垮 |
| D-33 | 长期竞品参照 | **主灵感锚点仍是《星露谷物语》；修仙经营与成长维度参考《鬼谷八荒》《觅长生》《了不起的修仙模拟器》《太吾绘卷》** | 这样能把"长期生活感"与"修仙系统深度"分开吸收，不会误把项目做成对某一款作品的表面拼贴 |
| D-34 | 空灵根（替换"绝灵之体"，**纯叙事层**） | **弃用自造词"绝灵之体"，改用真实灵根设定「空灵根」**（《从前有座灵剑山》王陆：万古一遇、吸收天下第一却全漏、净留存≈0）。序章测灵柱只读留存，把空灵根**误判为无灵根**——"空灵根"一词序章不点破，中期由萧无极/忘言叟带出（D-39）。偷天换劫诀原理="用天劫雷能封堵漏洞"。**漏斗/漏勺是运作比喻不是名字；吞吐量/漏失率/净留存是世界观说法不是代码字段**——落地只有既有 `bodyFoundation`（见 `03` §2.2.1、`09` §0）。 | 旧自造词（真空绝缘）静态、无出处、无叙事深度；空灵根是玩家一眼认得的经典梗，且让"测灵被拒"的误判逻辑自洽（读数与无灵根无异）；**零新系统**（比喻映射到已有 bodyFoundation） |
| D-35 | 神农设定 | **第一个穿越者体修 + 偷天换劫诀原作者 + 灵田改良者**；动机为回报凡人恩情（无心插柳造福苍生）；自愿匿名→凡人筑无面像；理论嵌入土地（安全锁）；主角灵田恰好是神农百万年前隐居地 | 参考观山！许然"无心插柳成为神农"；神农两面遗产（公开面=灵米改良→无面像；隐秘面=灵草研究→偷天换劫诀残卷）形成叙事闭环 |
| D-36 | 神农遗响线索（**MVP 只做叙事层，系统留 DLC**） | **MVP**：六段风味文字写进本就要做的 NPC 闲聊 / 物件描述 / 探索文本 + 一句话结局变体，玩家自行拼凑，零新系统。**后续补丁 / DLC**：收集计数 → 拼合 UI → 真相揭示 → 金色命名（灵田→「神农遗泽」等）+ 永久 Buff + 成就（见 `02` §2.8 标注 [DLC]）。 | 项目目标是无限靠近星露谷、只借修仙做背景（参考觅长生）；重收集系统偏离核心循环、增加开发负担，故拆分：留白叙事进 MVP，锦上添花进 DLC |
| D-37 | 灵米/灵草区分 + 贡赋制 | **灵米=凡人主食微含灵力+贡赋上供；灵草=体修丹材高药性灵力**；贡赋=凡人种灵米交保护费给宗门换保护 | 区分两层灵农业生态；贡赋制解释灵修不记录神农的原因；背景设定，非必须游戏机制 |
| D-38 | 太一宗修正 | **删除"禁绝偷天换劫诀"**；改为轻视但不迫害；功法近乎绝迹，不是被禁而是没人当回事保存 | 禁术设定增加开发复杂度且偏离核心循环；主角的敌人是天道，不是宗门 |
| D-39 | 空灵根"揭示时机"（序章不点破） | **序章只出现"无灵根"判定**；长老、主角、玩家都以为是无灵根，主角发出经典梗哀鸣"难道没有灵根就真的不能修仙了吗"。"空灵根"一词**中期才点破**：忘言叟隐约看穿（`02` §3.5）、萧无极再现时当面道破（`02` §3.3/§7.5）。 | 保留"废柴被拒→逆袭"的情感冲击与悬念；让玩家像主角一样"后知后觉"，呼应星露谷/觅长生的留白式揭示（玩家自行拼凑真相） |
| D-40 | 自动化解放农务（R3-B） | **引水阵**（水属性阵法，stage 4/5/6 分级自动浇水，对标星露谷洒水器 Lv2/6/9；复用阵法系统 `ArrayDef.type='water'` + 清晨 `applyFarmDayEnd` 自动浇）+ **守田兽 courier 专长自主代理**（日终自动拾取地面物归仓，扩 D-28/`docs/18 P2-2` courier）。**两阀守 `00 C5`**：①自动化不涨 `bodyFoundation`（只动 storage/vigor/tile，沿用 facility 范式）；②引水阵取最强+moisture cap、courier vigor 上限天然封顶（不引入 `automation` 递减参数块，Core Keeper 递减以 vigor+cap 替代）。stage 4+ 中期内容，不前移序章（守 `docs/02:88`）。 | 整改 `docs/21 §4.3` 降农务权重；deep-research 竞品铁律（洒水器分级/Junimo 0 XP/Core Keeper 递减）；设定合规（阵法体系 D-28 水脉延伸 + C5 凡人挣扎不被自动化掏空） |
| D-41 | 修仙核心主循环深化（R3-C） | **天劫短路修正（C1-a）**：`tryTribulation`（main.ts）不再 `runTribulation(blockChance:0)`+`breakthrough` 立即短路，改走 D-27 `invoke-tribulation`→countdown→due 链（进 T_trib 日级准备窗，归零由 `resolveDueTribulation` 结算）。**炼丹四轴激活（C2）**：`resolveBrew` 加 `warningConflict` 字段（`conflictMagnitude > explosion×conflictRatio`，救活 `params.ts:192` 死参数，docs/06 §5.2/R7）+ `furnacePanel` 呈现亚阈警告。**明确推迟**（大工程）：C1-b 玩家逐雷操作渡劫大考（phase 状态机+逐雷 action+突破门禁）、C1-c D-06 擦弹接正式天劫、D-07 秒级实时雷场 UI、C2-c 涌现配方（同 inputs 多配方/序敏感）、inventoryUI HTML 四轴呈现（craft WIP 稳定后）。 | 整改 `docs/21 §4.4` + deep-research 维度 c（天劫阶段性大考/炼丹底层逻辑探索体系）；C1-a 修正 auto-resolve 实现偏差回归 D-27，C2 激活已有 R6 四轴能力（零内容增量） |

### D-30 补充约束（后终局生活模式）

- 进入 `留驻此界` 后，**境界、主修突破、飞升主线不再继续增长**，避免出现"拒绝飞升但继续无限修仙"的设定冲突。
- 保留并强化的，是**后终局生活向发展线**：农庄扩建、守田兽巡守、阵法护场、灵草珍藏、村镇关系、节庆、遗迹余波治理、人间巡守责任。
- 后终局不照搬《星露谷物语》的婚育/社区中心尾盘，而是突出本项目特色：
  - `凡骨归田`：采菊东篱式的灵田、药圃、暖棚、茶棚与四时节律。
  - `镇守人间`：玩家以高阶体修身份留在人间，处理妖兽侵田、残脉失衡、散修求援、节令灾异。
  - `阵武合一`：以阵法控场 + 体修近战 + 守田兽巡守构成后终局稳定循环。
- 该模式是**胜利后的可持续游玩层**，不是新一轮高境界数值爬塔。核心目标从"逆天破境"切换为"守住来之不易的人间日常"。

### D-31 / D-32 / D-33 补充约束（平台与长期发展）

- 浏览器版是**首发演示与自动化验证载体**，不是对项目终态平台的限制性定义。
- 核心代码组织必须持续维持 `sim` 与平台 IO/渲染的边界，保证同一套离线单机规则既能支撑浏览器试玩，也能继续支撑桌面端封装。
- 首个公开版本仍以 `MVP 必要项` 为准，不为了“更像完整大作”而在首版阶段引入失控的内容体量。
- 后续更接近《星露谷物语》的深度，不通过首版横向堆功能实现，而通过 `Patch / DLC / 大版本` 分期补深农庄空间、关系系统、长期经营与后终局生活层。
- 不以任何平台扩展为理由恢复联网/服务器依赖；桌面端与未来移动端默认仍遵守离线单机边界。

---

## 3. 待办编辑清单（Reconciliation Edit Checklist）

> 全部 agent 落地后，按此清单一次性应用（保持文档自洽）。
> **状态（2026-07 复核）**：R1–R10 的机制/数值裁定**已全部落地**到 05/06/14（已核实：`baseDamage=12+8×stage`、炸炉阈 `14+2×stage`、丹毒 `2.0/日`、7 阶阶段表、乘性 targeting、四轴/一维投影均在正文）。以下勾选反映实际落地情况。

- [x] **`14`**：§8.1 阶段表已扩 7 阶（对齐 09）；§5.2 conductivity 已改 0.1–1.8；§6.2 已注明并入 ExposureCoeff/QualityBonus；P001–P037 覆盖 R1–R10。
- [x] **`05`**：§1.2 倒计时已改"日级准备窗 + 秒级临战"（引 14 T_trib）；§3.2 权重乘性；§4.2 TemperingGain 统一式（R3/R10）；§4.3 baseDamage=12+8×stage（R9）；§5.2 雷类型按 7 阶。
- [x] **`06`**：§2 已补"四轴内部/一维投影"（R6）；§1.2 衰减 2.0/日（R8）；§5.2 炸炉阈 14+2×stage（R7）。（§13 中医七情配伍映射：如未落地则保留为待办。）
- [x] **`08`**：§3.3/§5 导电性表 0.1–1.8（R5），与 05 乘性权重一致。
- [x] **`11`**：`Tile.conductivity` 倍率语义、`PropertyVector` 一维投影规则已注。
- [ ] **`19`**：开放问题改为"已决策（见 20 §2）"；保留真正未决项。
- [ ] **`00-README.md`**：决策日志补 D-03..D-23；§7 状态更新。

> 叙事侧后续裁定（2026-07 本轮审计落地）：D-38 残留（偷天诀"教义禁忌"措辞）已在 02/03 改为"个人执念·非宗门禁令"；神农"体修之源"收窄为"主动引劫这一支的开创者"（D-35）；"真·凡骨归田"触发对齐 D-30（扛过紫雷后留驻此界）；残卷首页三版统一为 02 §7.3 正典。

---

## 4. 待 C/D 落地后补充的决策

> 待 `13/16/17/18/19` 落地，可能新增的开放项在此预登记，落地后裁定。

- 美术方向（程序化 vs 手绘）—— 倾向：程序化优先 + 极简水墨占位（13）。
- 最小交易系统细节（16）—— 倾向：仅游方散仙事件触发，无常驻商店。
- 蒙特卡洛调参的目标代理指标阈值（17）—— 倾向：首劫存活率 60–75%、通关 15–25h（已记 14/README）。
- MVP 垂直切片边界（18）。
