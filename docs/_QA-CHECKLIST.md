# _QA-CHECKLIST · 设计包终审清单（critic 用）

> 任务 #9 的执行依据。逐条核对，未通过则补全/修正，直到整套设计包"完整、自洽、可直接进入 vibe coding"。
> 本文件是过程工件（下划线前缀），不属于正式交付文档树。

---

## A. 完整性（每份文档该有的都有）

- [ ] `02` 四幕全展开 + 每节点"剧情→玩法解锁"映射 + 储物戒开启机制 + 多结局/失败态叙事。
- [ ] `03` 世界规则 + 天劫天道宇宙观 + 灵草/丹方/阵法 lore + 每天象事件的世界观文字 + 历法 + 美术锚点。
- [ ] `04` 流程FSM + 画面清单 + 双模HUD + 丹炉UX + 控制表 + 可访问性 + onboarding + juice表。（已成，复审）
- [ ] `05` 倒计时规则 + 模式切换 + targeting算法 + 阵法系统 + 淬体公式结构 + 控血曲线 + 阶段scaling + 紫雷劫 + 边界 + 【可调参数】标注。
- [ ] `06` 丹毒模型 + 药性数值化(四气) + 槽位 + 火候 + 平衡条 + 非线性产出 + 丹方/残谱 + 炸炉后果 + 边界 + 【可调参数】。
- [ ] `07` 权重引擎(可种子化) + 因果链状态机 + 3示例扩写 + 3–5新事件 + 玩家应对空间 + 事件联动 + 数值钩子。
- [ ] `08` 流程 + 灵气生长公式结构 + 土壤+导电性(耦合05)+季节+地形+种子循环。
- [ ] `09` 阶段阶梯 + 经脉子进度 + 突破机制 + 功法进度耦合 + 难度曲线 + 终局 + 失败/软重置建议。
- [ ] `10` 语言决策矩阵(含明确推荐) + 渲染 + 固定步长循环 + ECS vs OOP + 分层图 + 确定性策略 + RNG分流 + 性能预算 + 构建。
- [ ] `11` 全实体字段表 + ER关系 + 序列化/迁移 + 数据驱动 + 确定性快照。
- [ ] `12` 目录树 + 依赖清单(含备选) + 构建 + 内容管线+schema校验 + CI + 零干预开发循环。
- [ ] `13` 程序化优先 + 资产清单 + 调色板 + 音频模型 + 来源管线 + 可访问性(光敏/色盲)。
- [ ] `14` 各系统公式(生长/灵气/丹毒/药性平衡/targeting/damage/tempering/突破/事件权重) + 进度曲线表 + 调参哲学 + 【平衡参数注册表】总表。
- [ ] `15` 灵草/丹方/丹药/奇遇/物品 各 15–30 行真实数据(非占位)。
- [ ] `16` 资源流图 + 稀缺模型 + 通胀控制 + 储物戒容量 + 交易最小化决策 + 死亡经济 + 自动化监控指标。
- [ ] `17` 测试金字塔 + 逻辑/渲染解耦 + 无头harness + 蒙特卡洛调参 + Stratabots分层 + 属性测试 + 黄金回放 + 自动调试闭环 + CI + 好玩代理指标。
- [ ] `18` MVP垂直切片定义 + M0–M6里程碑(交付物/退出标准/工作量) + 并行 + 风险前置。
- [ ] `19` 技术/设计/内容/无人干预专项风险 + 开放问题清单(选项+推荐+理由)。

## B. 一致性（跨文档）

- [ ] **术语**：全包统一用 `00-DESIGN-BRIEF.md §6` 双语术语（如 PillPoison/TemperingBuff/SpiritHerb 等大小写一致）。
- [ ] **数值对齐**：`05–09` 的 `【可调参数】` 与 `14` 参数注册表一一对齐（无悬空参数、无重复定义冲突）。
- [ ] **实体对齐**：`11` schema 字段与 `05–09/15` 用到的属性一致（如 SpiritHerbDef 含导电性/药性向量/tier）。
- [ ] **文件名**：所有交叉引用的文件名实际存在（`ls docs/` 核对）。
- [ ] **机制↔UI**：`04` 描述的 HUD/画面与 `05–09` 机制一一可对应（倒计时、平衡条、阵法高亮等）。
- [ ] **路线图↔机制**：`18` 里程碑解锁的系统与 `02` 四幕、`09` 阶段一致。

## C. 事实底座落地（关键洞见须在文档中体现）

- [ ] `06` 炼丹：是否纳入 **中医七情配伍**（相须→增效/相使→辅效/相畏相杀→净毒/相恶→废丹/相反→炸炉）与寒热温平(+平)四气数值化；是否参考 **Potion Craft 空间化炼丹图**隐喻（药性向量+火候锁定）。
- [ ] `10` 架构：是否推荐 **TypeScript**（或给出同等充分理由的替代）+ 固定步长 + **记录输入而非状态**回放 + 种子化PRNG分流；ECS 是否考虑 bitECS/Becsy。
- [ ] `17` 测试：是否纳入 fake clocks / golden schedules / 快照对照 / 蒙特卡洛(Silver/Bakkes/Stratabots/Volz) / 属性测试。
- [ ] `08` 种田：是否参考星露谷"天数分阶段+肥料跨季+收获定品质+多茬"惯例并加灵气维度。

## D. Vibe-Coding 就绪度（退出标准）

- [ ] 是否有明确的**技术栈终审决策**（语言/渲染/ECS）。
- [ ] 是否有**参数注册表**供自动调参消费。
- [ ] 是否有**无头模拟 + 蒙特卡洛调参**的可执行设计。
- [ ] 是否有**零干预开发单元 SOP**（数据→纯函数→测试→模拟→回归）。
- [ ] 是否明确**须人类拍板项**已收集（存档哲学/难度目标/交易/范围/美术）。
- [ ] 是否无"假完成"（无 TODO 占位、无"待补"、无未实现分支的空文档节）——除明确标注的"待你拍板"项外。

## E. 执行方式

1. 等 4 个 agent 全部返回。
2. `ls docs/` 核对文件名与 A 清单。
3. 逐文档 Read + 按 A/B/C/D 打勾/记缺陷。
4. 缺陷分两类：**可自补**（主线直接 Edit 补全）vs **需 agent 追加**（SendMessage 带上下文续派）vs **须用户拍板**（记入 19）。
5. 补全后跑一遍 B 一致性终检，更新 `00-README.md §7` 状态为 ✅。
